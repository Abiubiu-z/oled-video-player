/**
 * ============================================================================
 *  串口驱动 - 头文件
 * ============================================================================
 *  提供串口初始化、发送字节的函数声明
 * ============================================================================
 */

#ifndef __SERIAL_H
#define __SERIAL_H

#include "stm32f10x.h"

void Serial_Init(void);             // 初始化USART1（921600bps, 8N1, 接收中断）
void Serial_SendByte(uint8_t Byte); // 发送单字节（阻塞式，调试用）

#endif
